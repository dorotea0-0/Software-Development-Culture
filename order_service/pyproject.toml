[project]
name = "order-service"
version = "0.1.0"
description = "Application for servise user"
authors = [
    {name = "Kirill"}
]
license = "MIT"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "uvicorn[standard] (>=0.46.0,<0.47.0)",
    "httpx (>=0.28.1,<0.29.0)"
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
